<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njust.roftwaremanage.LabManagement.dao.ClassroomMapper">
    <!-- 添加Emp数据,parameterType -->
    <select id="findClassroomByAddress" parameterType="String" resultType="com.njust.roftwaremanage.LabManagement.entity.Classroom">
        select * from classroom where address=#{address}
    </select>
    
</mapper>